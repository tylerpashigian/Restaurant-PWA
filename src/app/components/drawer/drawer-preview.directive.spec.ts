import { DrawerPreviewDirective } from './drawer-preview.directive';

describe('DrawerPreviewDirective', () => {
  it('should create an instance', () => {
    const directive = new DrawerPreviewDirective();
    expect(directive).toBeTruthy();
  });
});
